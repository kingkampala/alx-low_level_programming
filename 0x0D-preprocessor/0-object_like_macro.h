#include <stdio.h>
#include <stlib.h>

#define SIZE 1024

/**
 * main - Entry point
 * defines a macro named SIZE
 * as an abbreviation for the token 1024
 * Return: Always 0
 */
int main(void)
{
	int x = SIZE;
	return (0);
}
